import React from 'react';
import { Text, View} from 'react-native';

const Camera = () => {
    return(
        <View>
            <Text>camera...</Text>
        </View>
    )
}

export default Camera;